<button class="btn btn-sm pull-right oc-logout" ng-click="doLogout()">
    log out
</button>
<span class="oc-welcome pull-right">welcome
    <b>{{account.user.username}}</b>, you're plan is
    <b>{{account.plan.name}}</b>
</span>