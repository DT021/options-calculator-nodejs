<span ng-class="{ 'oc-select-wrapper': ! status.whatif }"
      ng-disabled="menuService.isDisabled('select','in',this)">
    <select class="oc-dropdown oc-strat-dropdown"
            ng-options="strat.name group by strat.optionsContract.symbol for strat in strategies track by strat.name"
            ng-disabled="status.whatif"
            ng-change="doUpdatePositions()"
            ng-model="strategy"></select>
</span>